package main.java.practice.week6.sun;

import java.util.Arrays;
import java.util.List;

public class Collectors {

    public static void main(String[] args) {

        List<String> newWords2 = Arrays.asList("мама", "мыла", "раму", "мама", "чисто");
       // newWords2.stream().map(String::length).collect(java.util.stream.Collectors.summingInt(p -> % 2 == 1 ? p : 0));

       // newWords2.stream().map(String::length).


    }
}
